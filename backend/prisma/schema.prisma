// This is your Prisma schema file
// Full schema will be implemented in TASK-002

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Placeholder model - full schema in TASK-002
model HealthCheck {
  id        String   @id @default(uuid())
  status    String
  createdAt DateTime @default(now()) @map("created_at")

  @@map("health_checks")
}
